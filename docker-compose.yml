version: '3.8'

services:
  pong-game:
    build: .
    ports:
      - "8080:8080"
    container_name: pong-game-container
    environment:
      - NODE_ENV=production
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped